Linux中创建用户与修改密码：

adduser 用户名
passwd 密码
groupadd 工作组名

userdel 用户名

cat /etc/passwd 查看当前系统中的所有用户
cat -n filename    -n行号

cat一次会把所以内容显示出来，
more可以一页一页的显示，head/tail -n linenum filename
less 比more更强大 （/搜索的内容，继续搜索再输入/；上下左右可移动页面，空格：下一页，B：上一页；Q：退出）
grep 查找的内容 filename  (格式：grep  [选项]  ”模式“  [文件])
find [option] [path...] [expression] (find命令是根据文件的属性进行查找，如文件名，文件大小，所有者，所属组，是否为空，访问时间，修改时间等。)
locate [OPTION]... PATTERN...   (locate与find差不多，只是比find快)


whatis [command] (man过于啰嗦,而whatis用一句话解释命令的作用 )

1. root修改普通用户的密码：（root登陆）
sudo passwd user_name

3. 普通用户修改自己的密码：（普通用户身份登陆）
passwd



文件删除与显示：
ls 显示文件 （-l 以列表的形式显示查询出的文件）
ls -a  显示所有文件包括隐藏文件  （ls -la或ll -a）
l.或者ls -d .*    只显示隐藏文件
du -b filepath 参数-b表示以字节计数    查看文件大小

rm -i 文件夹 （递归删除文件夹下的所有文件和子目录）
rm -f 文件名    (只是文件)
rm -fr .* (删除当前目录下的所有隐藏文件)  rm -fr 文件名 （目录或者文件）
mv file1 file2    更换文件名

ln -s Targe Link_name  (符号连接（软连接），相当于Windows中的快捷方式)
ln Targe Link_name    (硬连接，形成两个相同的文件，两个文件会被同时修改)

输出重定向： > filename 
                    >> filename  同样文件名不覆盖
管道"|"是实现“将前面的标准输出作为后面的标准输入”

gzip gunzip
tar -cvf filename.tar 目录名
tar -xvf filename.tar

wget [OPTION] [URL]  下载命令 -c参数, 可以断点续传, 如果不小心终止了, 可以继续使用命令接着下载

环境变量设置：https://blog.csdn.net/LLZK_/article/details/53813266
	永久级：
		vim（root）在/etc/profile文件中添加我们想要的环境变量，用export指令添加环境变量
		注意：添加完成后新的环境变量不会立即生效，除非你调用source /etc/profile 该文件才会生效。否则只能在下次重进此用户时才能生效。
	
	用户级：
	    设置方法：在用户主目录”~”下的隐藏文件 “.bash_profile”中添加自己想要的环境变量。
	    利用vim打开文件，利用export添加环境变量。同样注意，添加完成后新的环境变量不会立即生效，
		除非你调用source ./.bash_profile 该文件才会生效。否则只能在下次重进此用户时才能生效。
		.bashrc和.bash_profile，原则上来说设置此类环境变量时在这两个文件任意一个里面添加都是可以的。
			
	临时有效的环境变量（只对当前shell有效）：
		我们退出登录或者关闭终端再重新打开时，这个环境变量就会消失。是临时的。
        设置方法：直接使用export指令添加。 
		
	设置环境变量常用指令：
		echo &PATH    查看显示环境变量
		export   设置新的环境变量    export 新环境变量名=内容    例:export MYNAME=”LLZZ”
		修改环境变量没有指令，可以直接使用环境变量名进行修改。   例：MYNAME=”ZZLL”
		env		查看所有环境变量
		set		查看本地定义的所有shell变量
		unset		删除一个环境变量   例 unset MYNAME
		readonly	设置只读环境变量。 例：readonly MYNAME



1、rpm包安装的，可以用rpm -qa看到，如果要查找某软件包是否安装，用 rpm -qa | grep “软件或者包的名字”。
1
[root@hexuweb102 ~] rpm -qa | grep ruby
2、以deb包安装的，可以用dpkg -l能看到。如果是查找指定软件包，用dpkg -l | grep “软件或者包的名字”；
1
[root@hexuweb102~]dpkg-l|grepruby
3、yum方法安装的，可以用yum list installed查找，如果是查找指定包，命令后加 | grep “软件名或者包名”；
1
[root@hexuweb102 ~] yum list installed | grep ruby
4、如果是以源码包自己编译安装的，例如.tar.gz或者tar.bz2形式的，这个只能看可执行文件是否存在了，
上面两种方法都看不到这种源码形式安装的包。如果是以root用户安装的，可执行程序通常都在/sbin:/usr/bin目录下
